{
	name "${pkg.name}"
	version "${pkg.version}"
    description "${pkg.description}"
	author "${pkg.author}"
	license "${pkg.license}"
	
    { scripts
		test "echo \"Error: no test specified\" && exit 1"
    	$include v08/app/package/server/scripts
	
    { dependencies
		$include v08/app/package/server/dependencies

	{ devDependencies
		$include v08/app/package/server/devDependencies