$group
	$params name

	var ${name}
		iife
            param K
            param State
            param Pub
            param Sub

            class _${name}
                super React.Component

                $hook

            
            return _${name}
			
			(
                @ bxKodes
                @ bxState
                @ PubSub.publish
                @ PubSub.subscribe